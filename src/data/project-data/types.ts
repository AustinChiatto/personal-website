export type ExternalLinkProps = {
  label: string;
  href: string;
  favicon: string;
};

export type TeamMemberProps = {
  label: string;
  href: string;
  icon: string;
};

export type TechStackProps = {
  label: string;
  desc: string;
  icon: string;
};

export type ArticleProps = {
  markdownSection: Array<string>;
};
